# APP_STAGE=dev   # dev (default) | staging | main — selects src/config/stages.json's active entry
DATABASE_URL=postgres://postgres:postgres@localhost:5480/gp5_library
STORAGE_DIR=./storage
JWT_SECRET=change-me
JWT_EXPIRES_IN=7d
# CORS_ALLOWED_ORIGINS=http://localhost:4200   # comma-separated; required for staging/main